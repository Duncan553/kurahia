// Re-export shim — see WaiterTabDetailScreen.tsx. The manager reads the same
// screen as the owner; the API decides which rows each of them may see.
export { default } from '@shared/screens/SuggestionsScreen'
