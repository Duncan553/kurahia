.venv/
__pycache__/
*.pyc
.env
.env.production
instance/
*.db
*.sqlite3
.pytest_cache/
dist/
build/
*.egg-info/

# Dev-only seed scripts (contain plaintext passwords — never commit)
test_setup.sh

# PWA build artifacts — global rules, NOT per-app.
# The old list named employee_pwa and owner_pwa explicitly; shared_ui and
# station_pwa were added to the project later and nobody appended them, so
# 8,218 files of shared_ui/node_modules got committed. These match at any depth.
node_modules/
.vite/

# Generated test output
tests/playwright/sweep-results/
tests/playwright/screenshots/
playwright-report/
test-results/

# Working documents — kept on disk, not in the repo.
# One-off audits, session logs, research notes and the Ivy Document's 15MB of
# screenshots. They are useful locally and were noise in the history: 121 files
# nobody reads from GitHub. Untracked with `git rm --cached`, so every file is
# still sitting in docs/ on this machine.
#
# To bring one back: git add -f docs/THE_FILE.md
docs/
